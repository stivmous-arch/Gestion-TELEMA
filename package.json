{
  "name": "telema-gestion-android",
  "version": "14.5.0",
  "description": "Gestion TELEMA - Diocese de Dolisie (application Android, fonctionne avec ou sans internet)",
  "scripts": {
    "build-local": "npx cap add android && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
